//flutter core
import 'package:flutter/material.dart';

//TODO 3 : CREATE ANALYSIS SCREEN WITH GRAPH AND A MONEY METER
class AnalysisScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Analysis Screen')),
    );
  }
}
