//flutter core
import 'package:flutter/material.dart';

//TODO 2 : CREATE AN INCOME SCREEN THAT ALLOWS USER TO ENTER THERE TOTAL INCOME PER MONTH
class IncomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Income Screen')),
    );
  }
}
