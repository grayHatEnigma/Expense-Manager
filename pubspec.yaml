name: expense_manager
description: an awesome expense manager application using flutter

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  # other dependencies
  date_format: ^1.0.8
  provider: ^4.0.1
 

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true



  fonts:
    - family: 'Tajawal'
      fonts:
        - asset: fonts/Tajawal-Regular.ttf
